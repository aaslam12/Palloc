# Diagrams

The primary allocator architecture diagram is embedded directly in the README as Mermaid Markdown so GitHub can render it without generated image files.

Place exported diagram images here only when a diagram cannot be represented cleanly in Mermaid.
